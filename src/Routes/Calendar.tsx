import { Box } from "@chakra-ui/react";

// Somehow integrate this with a google calendar?
// Which libraries to use?

// Show when I am available.
export const Calendar = () => {
  return <Box>I am available sometimes hehe</Box>;
};
